require 'spec_helper'

describe ParamsTransformer do
  it 'has a version number' do
    expect(ParamsTransformer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
